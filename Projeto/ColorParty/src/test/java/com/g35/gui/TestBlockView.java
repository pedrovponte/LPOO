package com.g35.gui;

import com.g35.data.BlockModel;
import com.g35.data.Color;
import com.g35.data.Position;
import org.junit.Test;
import org.mockito.Mock;
import org.mockito.Mockito;
import org.mockito.stubbing.OngoingStubbing;

public class TestBlockView {
    @Test
    public void testDraw() {
        /*BlockView blockView = Mockito.mock(BlockView.class);
        BlockModel blockModel = Mockito.mock(BlockModel.class);
        blockView.changed(blockModel);
        Mockito.verify(blockView, Mockito.times(1)).draw(blockModel);*/
    }
}
